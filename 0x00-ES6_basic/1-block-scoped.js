/* eslint-disable */

export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;

  // The if block is no longer necessary since you want the same values returned
  return [task, task2];
}
